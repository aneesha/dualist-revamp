"""
Models for Active Learning app.

Since this app interfaces with the FastAPI backend,
we don't need complex Django models. The state is managed
by the backend.
"""

from django.db import models

# No models needed - state is managed by FastAPI backend
